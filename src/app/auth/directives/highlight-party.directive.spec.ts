import { HighlightPartyDirective } from './highlight-party.directive';

describe('HighlightPartyDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightPartyDirective();
    expect(directive).toBeTruthy();
  });
});
